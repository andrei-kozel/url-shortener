package shorten_test

import (
	"testing"
)

func TestService_Shorten(t *testing.T) {
	t.Run("Generate shortening to a given URL", func(t *testing.T) {
		t.Skip("Not implemented")
	})

	t.Run("Uses custom identifier if provided", func(t *testing.T) {
		t.Skip("Not implemented")
	})

	t.Run("Returns an error if the identifier is already exists", func(t *testing.T) {
		t.Skip("Not implemented")
	})
}
